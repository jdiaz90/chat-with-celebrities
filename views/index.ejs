<h1 class="mb-4">Elige una persona famosa para conversar</h1>

<div class="list-group">
  <% Object.entries(celebrityMap).forEach(([id, data]) => { %>
    <a 
      href="/generate/<%= id %>" 
      class="list-group-item list-group-item-action d-flex align-items-center"
    >
      <img 
        src="<%= data.avatar %>" 
        alt="<%= data.name %>" 
        class="avatar me-3"
      >
      💬 Habla con <strong><%= data.name %></strong>
    </a>
  <% }) %>
</div>
